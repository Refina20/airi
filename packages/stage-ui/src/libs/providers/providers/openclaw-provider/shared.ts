/**
 * Default OpenClaw Gateway base URL for OpenClaw (Tina) provider.
 * Points to the CORS proxy which forwards to the OpenClaw Gateway.
 * Use 127.0.0.1 for local access, or Tailscale IP for remote.
 */
export const DEFAULT_BASE_URL = 'http://127.0.0.1:18790/v1'
